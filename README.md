# 🤖 ربات تلگرام مدیریت فروشگاه با هوش مصنوعی

ربات هوشمند تلگرامی برای مدیریت محصولات، دسته‌بندی‌ها، برندها و ویژگی‌های فروشگاه آنلاین با استفاده از Claude AI.

## ✨ قابلیت‌ها

- ✅ افزودن محصول به زبان ساده فارسی
- ✅ ویرایش و حذف محصولات
- ✅ مدیریت دسته‌بندی‌ها
- ✅ مدیریت برندها
- ✅ مدیریت ویژگی‌های محصولات
- ✅ جستجوی هوشمند محصولات
- ✅ نمایش لیست و جزئیات محصولات
- ✅ اتصال مستقیم به دیتابیس MySQL موجود

## 📋 پیش‌نیازها

- Python 3.8 یا بالاتر
- MySQL 8.0
- دیتابیس ecommerce موجود
- توکن ربات تلگرام (از [@BotFather](https://t.me/BotFather))
- کلید API Anthropic Claude (از [console.anthropic.com](https://console.anthropic.com))

## 🚀 نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 2. تنظیمات

فایل `.env.example` را کپی کرده و نام آن را به `.env` تغییر دهید:

```bash
cp .env.example .env
```

سپس اطلاعات زیر را در فایل `.env` وارد کنید:

```env
# توکن ربات تلگرام
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# کلید API Anthropic
ANTHROPIC_API_KEY=sk-ant-xxxxx

# اطلاعات دیتابیس
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ecommerce
DB_USER=your_username
DB_PASSWORD=your_password

# شناسه کاربران مجاز (Admin User IDs)
ADMIN_USER_IDS=123456789,987654321
```

### 3. دریافت User ID تلگرام

برای دریافت User ID خود:
1. به ربات [@userinfobot](https://t.me/userinfobot) پیام دهید
2. User ID خود را در فایل `.env` وارد کنید

### 4. اجرای ربات

```bash
python bot.py
```

## 📖 نحوه استفاده

### دستورات پایه

- `/start` - شروع کار با ربات
- `/help` - نمایش راهنما
- `/products` - لیست محصولات
- `/categories` - لیست دسته‌بندی‌ها
- `/brands` - لیست برندها

### مثال‌های کاربردی

#### 📦 افزودن محصول

```
یک محصول گوشی سامسونگ A54 با قیمت 15 میلیون تومان توی دسته موبایل اضافه کن
```

```
محصول لپ‌تاپ ایسوس با قیمت 25000000 تومان و موجودی 5 تا اضافه کن
```

#### ✏️ ویرایش محصول

```
موجودی گوشی آیفون 13 رو به 10 تا تغییر بده
```

```
قیمت لپ‌تاپ ایسوس رو 20 میلیون کن
```

#### 🗑️ حذف محصول

```
محصول هدفون سونی رو حذف کن
```

#### 🔍 جستجو و نمایش

```
محصولات موبایل رو نشون بده
```

```
جزئیات محصول گوشی سامسونگ رو نشون بده
```

```
دنبال گوشی اپل بگرد
```

#### 📂 مدیریت دسته‌بندی

```
یک دسته‌بندی به نام لوازم جانبی اضافه کن
```

```
لیست دسته‌بندی‌ها رو نشون بده
```

#### 🔖 مدیریت برند

```
برند اپل رو اضافه کن
```

```
لیست برندها رو نشون بده
```

## 🗂️ ساختار پروژه

```
rhop-bot/
│
├── bot.py              # فایل اصلی ربات
├── ai_handler.py       # پردازش هوش مصنوعی
├── database.py         # مدیریت دیتابیس
├── config.py           # تنظیمات
├── requirements.txt    # کتابخانه‌ها
├── .env.example        # نمونه تنظیمات
└── README.md          # راهنما
```

## 🗄️ ساختار دیتابیس

ربات با جداول زیر کار می‌کند:

- `products` - محصولات
- `categories` - دسته‌بندی‌ها
- `brands` - برندها
- `attributes` - ویژگی‌ها
- `attribute_values` - مقادیر ویژگی‌ها
- `helpers` - راهنمای محصولات
- `medias` - تصاویر و رسانه‌ها

## 🛠️ توسعه و سفارشی‌سازی

### افزودن عملیات جدید

برای افزودن عملیات جدید، مراحل زیر را دنبال کنید:

1. در `ai_handler.py` در system prompt، نوع action جدید را تعریف کنید
2. متد جدید در کلاس `AIHandler` بسازید
3. در متد `execute_action` شرط مربوطه را اضافه کنید

### تغییر مدل AI

در فایل `config.py`:

```python
AI_MODEL = "claude-sonnet-4-20250514"  # یا مدل دیگر
```

## ⚠️ نکات امنیتی

- هرگز فایل `.env` را commit نکنید
- User ID های مجاز را محدود کنید
- از کانکشن SSL برای دیتابیس استفاده کنید
- API Key های خود را محرمانه نگه دارید

## 🐛 رفع مشکلات

### خطای اتصال به دیتابیس

```
خطا در اتصال به دیتابیس: Access denied
```

**راه حل:** اطلاعات دیتابیس در `.env` را بررسی کنید.

### خطای توکن ربات

```
telegram.error.InvalidToken
```

**راه حل:** توکن ربات را از BotFather مجدد دریافت کنید.

### ربات پاسخ نمی‌دهد

1. بررسی کنید ربات در حال اجرا باشد
2. User ID خود را در `ADMIN_USER_IDS` اضافه کنید
3. لاگ‌های ربات را بررسی کنید

## 📝 لاگ‌ها

تمام عملیات در کنسول لاگ می‌شوند:

```
INFO - ربات در حال اجرا است...
INFO - پیام از کاربر 123456789: یک محصول اضافه کن
INFO - عملیات موفق: add_product
```

## 🤝 مشارکت

برای مشارکت در توسعه این پروژه:

1. Fork کنید
2. Branch جدید بسازید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. Push کنید (`git push origin feature/AmazingFeature`)
5. Pull Request باز کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 💡 ایده‌های توسعه آینده

- [ ] پشتیبانی از آپلود تصاویر محصول
- [ ] ایجاد گزارش‌های فروش
- [ ] مدیریت سفارشات
- [ ] اعلان‌های خودکار برای محصولات ناموجود
- [ ] پنل وب ادمین
- [ ] پشتیبانی از چند زبان
- [ ] یکپارچه‌سازی با سیستم‌های پرداخت

## 📞 پشتیبانی

برای سوالات و پشتیبانی:
- GitHub Issues
- تلگرام: [@your_username]

---

ساخته شده با ❤️ برای مدیریت آسان فروشگاه‌های آنلاین
