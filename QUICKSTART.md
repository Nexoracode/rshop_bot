# 🚀 راهنمای سریع شروع کار

## مراحل نصب در 5 دقیقه

### 1️⃣ نصب وابستگی‌ها

```bash
cd D:\project\python\rhop-bot
pip install -r requirements.txt
```

### 2️⃣ دریافت توکن ربات تلگرام

1. به [@BotFather](https://t.me/BotFather) در تلگرام پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام ربات را وارد کنید (مثلاً: My Shop Bot)
4. username ربات را وارد کنید (باید به bot ختم شود، مثلاً: myshop_admin_bot)
5. توکن دریافتی را کپی کنید

### 3️⃣ دریافت کلید API Claude

1. به [console.anthropic.com](https://console.anthropic.com) بروید
2. ثبت‌نام یا وارد شوید
3. از منوی سمت چپ روی "API Keys" کلیک کنید
4. "Create Key" را بزنید
5. کلید API را کپی کنید

### 4️⃣ دریافت User ID تلگرام

1. به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. User ID خود را یادداشت کنید

### 5️⃣ تنظیم فایل .env

```bash
copy .env.example .env
notepad .env  # یا با هر ویرایشگر دیگری
```

محتوای فایل:

```env
TELEGRAM_BOT_TOKEN=توکن_ربات_تلگرام_شما
ANTHROPIC_API_KEY=کلید_API_Claude_شما
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ecommerce
DB_USER=نام_کاربری_دیتابیس
DB_PASSWORD=رمز_عبور_دیتابیس
ADMIN_USER_IDS=User_ID_تلگرام_شما
```

### 6️⃣ تست سیستم

```bash
python test_bot.py
```

اگر همه چیز درست باشد، این پیام را خواهید دید:
```
✅ تمام تست‌ها با موفقیت انجام شد!
```

### 7️⃣ اجرای ربات

```bash
python bot.py
```

پیام زیر را باید ببینید:
```
INFO - ربات در حال اجرا است...
```

### 8️⃣ شروع کار با ربات

1. ربات خود را در تلگرام پیدا کنید
2. `/start` را ارسال کنید
3. پیام خوش‌آمدگویی را دریافت خواهید کرد

## ✨ اولین دستورات

پس از start، این دستورات را امتحان کنید:

```
/help
```

```
لیست محصولات رو نشون بده
```

```
یک محصول تستی با قیمت 1000 تومان اضافه کن
```

## 🔧 رفع مشکلات رایج

### مشکل: ربات پاسخ نمی‌دهد

**راه حل:**
1. مطمئن شوید User ID شما در `ADMIN_USER_IDS` است
2. بررسی کنید ربات در حال اجرا باشد
3. لاگ‌های کنسول را چک کنید

### مشکل: خطای دیتابیس

**راه حل:**
```bash
# تست اتصال MySQL
mysql -u username -p -h localhost ecommerce
```

اگر اتصال موفق بود، اطلاعات `.env` را دوباره بررسی کنید.

### مشکل: خطای AI

**راه حل:**
- کلید API Anthropic را بررسی کنید
- اعتبار API خود را در console.anthropic.com چک کنید
- اطمینان حاصل کنید اینترنت شما فعال است

## 📱 اجرای ربات در Background (Windows)

### روش 1: استفاده از pythonw

```bash
pythonw bot.py
```

### روش 2: ایجاد Service با NSSM

1. دانلود NSSM از [nssm.cc](https://nssm.cc/download)
2. نصب سرویس:

```bash
nssm install ShopBot "C:\Python\python.exe" "D:\project\python\rhop-bot\bot.py"
nssm start ShopBot
```

### روش 3: استفاده از Task Scheduler

1. Task Scheduler را باز کنید
2. "Create Basic Task" را انتخاب کنید
3. برنامه را روی `python.exe` و آرگومان را روی `bot.py` تنظیم کنید

## 🎯 مثال‌های کاربردی

### اضافه کردن محصول کامل

```
یک محصول با نام گوشی سامسونگ A54 با قیمت 15000000 تومان و موجودی 10 تا توی دسته موبایل و برند سامسونگ اضافه کن. وزنش 200 گرم و ارسال همان روز داره
```

### ویرایش چند فیلد

```
محصول گوشی آیفون 13 رو ویرایش کن. قیمتش رو 30000000 و موجودی رو 5 تا کن
```

### جستجو و فیلتر

```
تمام محصولات دسته موبایل رو نشون بده
```

## 💡 نکات مهم

1. **امنیت:** هیچ‌وقت فایل `.env` را commit نکنید
2. **بک‌آپ:** قبل از تست، از دیتابیس بک‌آپ بگیرید
3. **لاگ:** همیشه لاگ‌ها را برای debug نگه دارید
4. **محدودیت:** Claude API محدودیت rate limit دارد
5. **تست:** قبل از استفاده واقعی، حتماً تست کنید

## 📞 نیاز به کمک؟

- مستندات کامل: `README.md`
- تست سیستم: `python test_bot.py`
- Issues: در GitHub

---

موفق باشید! 🎉
