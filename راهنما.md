# 🤖 ربات فروشگاه - راهنمای نصب سریع

## 📦 نصب

### گام ۱: نصب کتابخانه‌ها
```bash
pip install -r requirements.txt
```

### گام ۲: ساخت فایل .env
فایل `.env.example` را کپی کنید و نام آن را به `.env` تغییر دهید:
```bash
copy .env.example .env
```

### گام ۳: تنظیم .env
فایل `.env` را باز کنید و اطلاعات زیر را وارد کنید:

```env
# توکن ربات از @BotFather
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# کلید API از console.anthropic.com  
ANTHROPIC_API_KEY=sk-ant-...

# اطلاعات دیتابیس MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ecommerce
DB_USER=root
DB_PASSWORD=your_password

# User ID تلگرام خود از @userinfobot
ADMIN_USER_IDS=123456789
```

## 🚀 اجرا

### تست سیستم:
```bash
python test_bot.py
```

### اجرای ربات:
```bash
python bot.py
```

## 💬 استفاده

بعد از اجرا، در تلگرام `/start` را به ربات بفرستید و شروع کنید!

### مثال‌های ساده:

```
لیست محصولات

یک محصول با نام گوشی سامسونگ و قیمت 15000000 تومان اضافه کن

موجودی محصول گوشی رو به 10 تا تغییر بده

لیست دسته‌بندی‌ها
```

## 📚 مستندات کامل

برای اطلاعات بیشتر:
- **README.md** - مستندات کامل
- **QUICKSTART.md** - راهنمای جامع

---

✨ **نکته:** همه درخواست‌ها را به فارسی ساده بنویسید!
